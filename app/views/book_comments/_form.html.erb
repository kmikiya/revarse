   <%= form_with(model: [@book, @book_comment], remote: true) do |f| %>
      <%= f.label :comment %>
      <%= f.text_area :comment, class: 'form-control' %>
      <%= f.submit "投稿" %>
      <% end %>